version: '3.8'

services:
  web:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
      - easyocr_models:/root/.EasyOCR
    restart: always

volumes:
  easyocr_models:
